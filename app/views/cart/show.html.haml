%main.container.general-main-wrap
  %h1.general-title-margin= t('cart')
  - if @order.order_items.any?
    = render 'orders/mobile_order_items'
    = render 'orders/order_items'
    = render 'coupon_section'
  - else
    .jumbotron.text-center.mb-0
      %p= t('no_items')